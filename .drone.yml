---
kind: pipeline
type: exec
name: test

platform:
  os: linux
  arch: amd64

steps:
  - name: test
    commands:
      - hostname


---
kind: pipeline
type: exec
name: test2

platform:
  os: linux
  arch: amd64

steps:
  - name: test2
    commands:
      - hostname



---
kind: pipeline
type: exec
name: test3

platform:
  os: linux
  arch: amd64

steps:
  - name: test3
    commands:
      - hostname

